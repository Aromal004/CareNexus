import React from 'react';

const Stats = () => {
    return (
        <div>
            <h2>Statistics</h2>
            <p>This is the stats section, shown by default.</p>
            {/* Add stats content as needed */}
        </div>
    );
};

export default Stats;
